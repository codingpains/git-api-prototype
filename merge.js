const diff3Merge =  require('diff3')

let diff3

function merge(version, commonAncestor, main) {
    diff3 = diff3Merge(version, commonAncestor, main)

    let res

    // Transform
    // test
    //
    return {}
}

module.exports = { merge }